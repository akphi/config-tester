{
  // this will prevent an incorrect invocation of tsc without -b from trying to build the
  // entire packages folder source files as one compilation (which will fail,
  // but drop a bunch of .js files in random places as an annoying side effect).
  "files": [],
  "exclude": [
    "node_modules",
    "dist",
    "build",
    "__tests__",
    "**/*.test*",
    "**/*.spec*"
  ],
  "references": [
    { "path": "./packages/lib1" },
    { "path": "./packages/component1" },
    { "path": "./packages/app1" }
  ]
}
