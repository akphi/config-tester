{
  "name": "@akphi/lib1",
  "version": "0.0.0",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf './build'",
    "dev": "tsc --build --watch"
  }
}
