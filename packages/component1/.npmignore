build
**/__mocks__/**
**/__tests__/**
# `package.json`, `CHANGELOG`, `LICENSE`, and `README` are never ignore 
# See https://npm.github.io/publishing-pkgs-docs/publishing/the-npmignore-file.html
/*.*
# We need resolve the full `tsconfig.json` so source navigation works properly on distributed package 
!tsconfig.json
